# Prosecution — Candidate defects

This is a concise evidence record, not an assertion that every candidate is
a defect. Target and base are fixed in [the manifest](00-manifest.md).

## Incentive

User requested five further lessons that deepen both RCCM source reading and
Bend mastery, with checked implementations and incremental local pushes.
The plan separates coordinates, units, space, flux balance and finite time.

## Falsification targets

| Candidate | Evidence to attack |
|---|---|
| One-index or inverse-direction cycle | All sixteen arbitrary matrix entries and the four independent axis contracts |
| Zero admitted by a positive domain | Successor numerator/denominator encoding and reciprocal content law |
| Focused/Condensed source conflation | Stress definition and the two documents' distinct stress-energy/density equations |
| Wrong derivative index or force sign | `T_xi`, positive x face convention, `gradient` versus conservative `gain` |
| Tautological cancellation | Whether `total` really adds the two computed gains instead of returning a cached boundary expression |
| Unsound polynomial certificate | Normalization soundness, live certificate evidence, true-denominator observation and real-operation bridge |
| Momentum reset or copied state | `advance`, affine `State`, structural fuel recursion and one-step versus all-fuel scope |
| Hidden untracked dependency | Clean archive gate and all new native demos/counterexamples |
| Standalone name collision | `units.bend` declares `Unit` after importing `Base` at this target |

## Simplicity and reversibility

The largest addition is the 390-line constructive arithmetic helper, rather
than runtime simulation machinery. Its extra proof surface requires explicit
review. The runtime remains a tiny exact reference; no backend or global
configuration was changed. Focused commits can be reverted without touching
the pre-existing absent cosmology checkout.
