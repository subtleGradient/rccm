import Base
import ./ledger.bend as L
import ./ledger-laws.bend as Laws

def Laws.capacity_contents(p, local, macro):
  {==}
def Laws.ambient_contents(p, local, macro):
  {==}
def Laws.local_contents(p, local, macro):
  {==}
def Laws.capacity_witness(budget):
  (L.capacity(budget), {==})
def Laws.nested_fixture():
  {==}
