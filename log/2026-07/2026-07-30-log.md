# RCCM Learning and Research Log — 2026-07-30

## Session goal

Turn the TauLab plan into a stateless, interruptible scientific specification
and implement the first bounded executable foundation.

## Durable marks

- Created `.ok/taulab.ok.md` as the canonical evergreen OK state and
  `.ok/taulab.ok.canvas` as its spatial map.
- Created a regenerable `.tasks/` hierarchy; chronology and completion state
  remain outside the evergreen specification.
- Wrote the six Quads-PREP artifacts. The working hypothesis now explicitly
  says the RCCM corpus is not yet a closed simulation specification.
- Implemented a TypeScript physical type system, immutable theory compiler,
  RCCM-v0 candidate bundle, intervention modes, checkpoint identity, and
  certification rules.
- Implemented and tested the deterministic C++23 CPU finite-volume foundation.
- Implemented the first Metal 3 constitutive kernel and compared it with the
  CPU oracle on the Apple M1.
- Recorded the executed evidence and open scientific gaps in
  `.tasks/artifacts/validation-20260730-foundation.md`.

## Scientific status

The formal and bounded engine-foundation tests pass. This does not establish
that RCCM-v0 is physically correct, fully discretized, or capable of producing
particles or forces. The strongest current claim is narrower:

```text
versioned theory contracts
→ executable dimensional/provenance gates
→ deterministic compressible-flow primitives
→ first CPU/Metal constitutive parity
```

The next numerical mark is passive evolution of the complete primary state
before any new force or emergence label is introduced.

## Learner-map update

The user is now working through RCCM as a buildable, falsifiable world rather
than only as a document to read. The TypeScript bridge is effective for
separating dimension, tensor shape, parameter provenance, causation, and
scientific authority. Future explanations should continue to place the
simulation terrain first, then attach equations and code contracts to each
boundary.

## Executable demo mark

The repository root now exposes `make demo`. The command compiles and runs a
live RCCM-v0 experiment whose story is:

```text
create a universe
→ measure a wave
→ sharpen the grid
→ replay and branch time
→ cross-examine Apple Metal with the CPU oracle
→ name the surviving claim and the open frontier
```

The committed-archive test passed without relying on local untracked files or
previous build products. The live Apple M1 run measured a tau-wave speed within
`2.00e-04` relative error, decreased field error across `32/64/128` cells,
replayed the full numerical state bit-for-bit, and matched the Metal
constitutive result to the double-precision CPU oracle within `6.64e-08`.

This is a communication milestone and an executable numerical milestone, not
an emergence milestone. It makes the current evidence exciting by making it
touchable and attackable; it does not promote RCCM-v0 to a validated physical
theory.

## Historical refractive-cosmology package map

Added `binyamintsadik-svg/Refractive_Cosmology` as the
`Refractive_Cosmology/` submodule, pinned at upstream commit `b965be0`.
Its February 10, 2026 code drop predates the July 30, 2026
`RCCM-Condensed.tex` snapshot, so the learning atlas now treats it as a
historical reproduction package rather than the current formal cosmology.

Every tracked script, the package README, runtime manifest, license, ignore
rules, and the complete 1,701-row Pantheon+SH0ES input were inspected. The
pipeline was also executed end to end from a disposable, space-free checkout.
The resolved audit environment was Python 3.12.13, NumPy 2.5.1, pandas 3.0.5,
SciPy 1.18.0, and Matplotlib 3.11.1.
Durable routing and provenance findings:

- current-claim precedence is TeX first, old code second;
- the inverse cloud derives both axes from the same observed `z` and `μ`;
- different branches use `zHD` versus `zCMB`, different scale constants, and
  different refractive-index formulas;
- the checked-in weighted verifier returned `K=3524.27 Mpc` and
  `eta=1.04778`, while the robustness figure hard-codes the corresponding
  unweighted fit (`K=3299.99 Mpc`, `eta=1.06833`);
- the two robustness plot scripts are byte-identical and neither consumes
  verifier output; and
- the package's radial proper-time integral is not the later TeX's direct
  `t(z)=τ₀/(1+z)` chronometer.

The root README now carries the complete file router and the operational
constraints for reproduction. `AGENTS.md` carries trigger-based instructions
so future work preserves the version boundary rather than silently merging
the February code with the July TeX.
