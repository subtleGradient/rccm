# 2026-09-20 — First Bend foothold

## Goal and scope

- User's stated destination: implement tau fluidics in Bend for games and
  simulations, while personally mastering the science.
- User requested the simplest directionally useful start in `./bend/`, not
  a from-scratch implementation of everything.
- Selected one frozen fluid sample: normalized speed maps to dynamic pressure
  fraction and residual static pressure fraction (`alpha_s_sq`).
- Deferred meshes, time integration, tensors, engine adapters and GPU work.
  Reused Bend `Base` rather than adding numeric infrastructure.

## Sources and boundary

- Read the focused TeX's section 2 ledger and the condensed TeX's
  `eq:admittance_pressure_ratio` derivation.
- Explicit restriction: zero macroscopic and shear loads, one translational
  speed. The implementation follows the squared admittance equation; the
  condensed glossary's ratio wording is not silently substituted.
- No claim of scientific validation or universal formal proof.

## Evidence and verification

- Bend 2.0.21 exists at `~/.bend/bin/bend`, outside this session's `PATH`.
- Both entry points type-check; emitted JavaScript runs under Node 24.1.0.
- The half-speed demo gives `0.25` used, `0.75` remaining. Ten regression
  cases pass, including rejection of negative, excessive and nonfinite input.
- A deliberately broken split in an in-memory copy of the emitted JavaScript
  is rejected with exit 1. Unmodified sources also build/run from `bend/`.
- `F32` primitives do not evaluate in the proof checker here. Tests therefore
  execute on the target and return a nonzero exit on failure.
- Native validation is blocked by the Apple compiler's unaccepted Xcode
  licence; no licence or system configuration was changed.

## Learner map and return

- No new evidence of mathematical mastery yet. Providing an implementation
  is not evidence that the user owns its prerequisites.
- Pending probe: predict the split at quarter speed, then explain the effect
  of doubling speed and the domain boundary.
- Next candidate: two sample locations and a static-pressure difference,
  keeping the difference separate from a gradient, force and time update.

## Follow-up — tensor-first, law-driven lesson planning

- User redirected the project toward a shared asymmetric metric tensor
  package with its own `LAWS.bend` / `PROOF.bend`, rather than pressure as the
  central abstraction. Requested a lesson plan before further implementation.
- Added `bend/asymmetric-metric-tensor/README.md`: jobs to be done for
  code-fluent/math-learning readers, nine incremental lessons, law ownership,
  exact-versus-numerical boundaries, and source obligations before motion.
- First proposed build: named axes and slot swapping with coordinate-swap
  laws and a universal double-swap law. A no-op implementation illustrates
  why double-swap alone is an insufficient specification. The full tensor
  component map remains visible from the outset. Pressure becomes the later
  capacity-to-diagonal bridge.
- No laws were frozen or proofs claimed; only the plan exists in the new
  folder. The existing numerical checks remain because they cover a different
  layer than the planned exact contracts.
- Inspected Bend 2.0.21's base types: no signed-integer or rational type was
  listed. Plan calls for auditing and adding minimal exact arithmetic only
  when the next lesson needs it, not building a math framework first.
- Recorded focused-source motion obligations: the density substitution in
  section 5.1 and the ambient-force admittance factor need explicit resolution
  before implementing time evolution.
- Direct execution now succeeds: `bend bend/main.bend` prints the expected
  split and `bend bend/tests.bend` passes all ten cases. Used the installed
  executable at `~/.bend/bin/bend`; the previous native licence blocker was
  no longer present. No system configuration was changed.
- Learning-map update: user prefers tensor-first relevance and law-driven
  construction. This is evidence of desired learning structure, not yet a
  demonstration of mathematical prerequisite mastery. The previous proposed
  next step of two pressure samples is superseded by the tensor-slot lesson.

## Lesson 1 — Find a slot

- User approved producing the first lesson. Added the shared `Axis` / `Slot`
  implementation and `row`, `column`, `swap` operations under
  `bend/asymmetric-metric-tensor/`.
- `LAWS.bend` states the agreed row exchange, column exchange and double-swap
  contracts. `PROOF.bend` imports them and proves all three for arbitrary
  slots by constructor cases and reflexivity. No new arithmetic is needed.
- `lessons/01-slots.md` keeps the full source component map visible, separates
  one physical sample from its sixteen component addresses, and connects a
  prediction, direct Bend run, types, notation, contract and proof.
- The runnable demonstration prints every address and `tx -> xt -> tx`.
- The no-op teaching module has a valid proof of double application being
  identity. Its separate counterexample is intentionally rejected because
  row `t` does not equal the required row `x`; it is not imported by the
  successful package gate.

### Verification

- Bend 2.0.21: the production gate prints `All terms check.`; the lesson
  demonstration runs natively; the no-op's weak proof passes.
- The deliberate counterexample exits 1 with a constructor equality mismatch,
  rather than a parser or import error.
- Isolated copies of the production package reject three implementation
  mutations with unchanged laws: a no-op, duplicated column, duplicated row.
  Removing a proof is also rejected as an incomplete proof. Production
  sources and the law file were not altered by these checks.
- The ten existing pressure regressions still pass.
- Lesson/navigation links and whitespace checks pass.
- An independent reviewer found no blocking issues in the proof scope,
  source-table signs, counterexample, or walkthrough.

### Learner map and return

- This is a delivered lesson, not evidence that the learner has completed it.
- Pending predictions: swap `xy`, swap a diagonal slot, distinguish address
  exchange from sign negation, and explain why the no-op passes the weak law.
- Next lesson remains signed pairs, contingent on the return probes rather
  than the green compiler result alone.

## Lesson 2 — Mirror or reverse

- User supplied successful direct runs of the lesson-1 demonstration and
  proof gate, then explicitly requested lesson 2. This confirms the run path;
  it does not by itself establish mastery of the previous prediction probes.
- Added a minimal exact signed-integer representation with one zero and
  positive/negative successor magnitudes, reusing Bend `Nat`. No arithmetic
  framework, fractions or floating-point operations were added.
- Added generic component-pair constructors plus pair transposition and
  negation. The pair container stores values, not validated axis assignments;
  those remain the future assembler's responsibility.
- Appended twelve contracts to `LAWS.bend`; all three lesson-1 law bodies
  remain unchanged. The new proof of `Aᵀ = -A` for generated pairs reuses
  double negation through Bend's equality rewrite.
- Added `lessons/02-pairs.md`, a direct-run demonstration and a deliberately
  rejected copying counterexample. The walkthrough keeps the full source
  matrix visible, separates generic symmetric pairs from the focused
  matrix's zero symmetric off-diagonals, and labels integer examples as
  sign exercises rather than complete physical states.

### Verification

- Bend 2.0.21 checks all fifteen package laws. The lesson runs directly,
  and scratch-only negative and zero input variants give the predicted pairs.
- Seven well-typed mutations were rejected by unchanged production laws:
  no-op negation, sign-reversing natural conversion, discarded symmetric
  input, copied antisymmetric input, discarded antisymmetric input,
  no-op transposition, and negating only one pair entry.
- Removing the antisymmetric-transpose proof is rejected as incomplete.
- Both intentional counterexamples fail on their expected equality claims;
  neither is imported by the successful package gate.
- Lesson 1's example and weak no-op proof, the ten pressure regressions,
  local navigation links and whitespace checks still pass.
- Independent review approved the exact representation, proof scope,
  source signs, rewrite explanation and educational boundaries.

### Learner map and return

- Proceeded by explicit request, without marking either lesson mastered.
- Pending probe: compare swapping a nonzero symmetric pair with negating it;
  reverse one source twist component; explain why zero hides the copying
  mistake and why a constant-zero constructor fails the content laws.
- Next planned slice is capacity and exact fractions for the diagonal,
  keeping positive capacity separate from the exhausted zero endpoint.

## Lessons 3–5 — Capacity, full assembly, and probes

### Request and observed learning edge

- User requested lessons 3, 4 and 5 together, with Git commits as work proceeds.
- The supplied terminal output confirms lesson 2 and the proof gate execute
  on the user's machine.
- The response to the `(-4,-4)` probe instead used `(-4,+4)`. Its operations
  were correct for that substituted antisymmetric state. The new walkthroughs
  ask the learner to write down the actual starting pair before operating.
  This is a narrow representation-tracking observation, not a general
  judgment of mathematical ability or evidence of completing the lessons.

### Delivered layers

- Exact noncanonical rationals represent `(positive-negative)/(1+d)`, with
  positive denominators. `Same` is cross-multiplication equality; structural
  record equality remains distinct. Arithmetic identities are proved with
  constructive Nat induction rather than assumed field axioms.
- Lesson 3: same-unit nonnegative pressure shares, positive-capacity state,
  input rejection at zero, numerator bound, exact budget and reciprocal laws.
- Lesson 4: a stored 4 × 4 matrix, an independent source table for every slot,
  complete Cartesian assembly, transposition, `S+A`, and unloaded baseline.
- Lesson 5: symmetric/skew recovery from stored entries and a genuine
  sixteen-term bilinear contraction. Its universal same-vector cancellation
  proof eliminates four diagonal terms and six mirrored pairs.
- One root `LAWS.bend` / `PROOF.bend` gate imports the layer contracts/proofs.
  All fifteen previous law bodies remain byte-for-byte unchanged.
- Each new lesson has a direct-run demonstration and a separately rejected
  counterexample. The recipes are algebraic samples, not solutions of a
  closed evolution equation or claims of physical admissibility.

### Checkpoints and verification

- Focused implementation commits: `012d307` (capacity), `58a3b0f` (assembly),
  `416edd9` (probes), alongside separate arithmetic and walkthrough commits.
- The gate checks 69 package laws after adding basis-constructor contracts.
- All five demonstrations run directly with Bend 2.0.21. All five deliberate
  negative fixtures fail on their expected claims, and the ten earlier
  numerical pressure regressions still pass.
- 168 compiled exact results agreed with independent Python `Fraction`
  calculations: all sixteen entries and both recovered parts for three
  samples, plus mixed/same-vector contractions with signed weights.
- Nine well-typed capacity/assembly/probe mutations were rejected with
  unchanged contracts, as was omission of the probe proof module. The
  arithmetic worker separately verified ten arithmetic mutations.
- Independent review found no blocking mathematical defect. It did catch an
  integration problem: the four arithmetic files were present live but
  absent from the merged commit's tracked tree. Commit `a694955` explicitly
  includes them; the reviewer verified the proof gate from its clean archive.
- Local lesson links and whitespace checks pass. No TeX or generated HTML
  source was changed; the pre-existing missing cosmology submodule was left
  untouched.
- A final clean `git archive` of `bfda193` passed the root proof gate, all
  five native demonstrations, all five expected-failure fixtures and the
  earlier pressure regressions. The committed package is self-contained.

### Explicit implementation limits and return

- Fractions are unreduced and can grow rapidly. A proposed extra closed
  numerical proof of the concrete full-probe result overflowed the checker
  stack. It was not presented as proved: the general symbolic gate remains
  checked, while the IO demo and independent exact oracle verify execution.
  No unsafe escape, tolerance, or assumed arithmetic law was substituted.
- Target overflow bounds, performance, numeric adapters, physical field
  compatibility and dynamics remain separate work.
- Pending probes: predict capacity-to-diagonal changes, distinguish full
  `U` from `S` and `A`, recover entries from the actual starting pair, and
  explain why a same-vector zero does not imply a zero antisymmetric tensor.
- Lessons are delivered, not marked mastered. The next planned lesson is
  explicit coordinate relabelling, not a premature field evolution solver.

## Next five — Plan and coordinate checkpoint

- User requested lessons 6–10, deeper Condensed-source and Bend mastery,
  implementation after planning, incremental commits and pushes to `local`.
- Started a new branch at local `main` (`d3f78e0`), which already contained
  landed lessons 1–5 and newer project instructions. No history rewrite;
  pre-existing absent `Refractive_Cosmology` remains untouched.
- Committed/pushed the explicit plan (`43eadfd`) before implementation:
  passive coordinates -> stress units -> differences -> shared-face balance
  -> fuel-bounded frozen-flux momentum experiment.
- Source audit separates the focused stress definition from the Condensed
  Rosetta mapping. The focused `rho_dyn`/`rho_eff` argument and Condensed
  admittance cancellation do not supply one unambiguous closed solver.
- Lesson 6 now implements a new-to-old orientation-preserving spatial cycle
  on arbitrary stored matrices. Ten additional contracts specify all axes,
  both indices, return after three cycles, and structural symmetry/skew
  preservation. Root gate includes them; no previous law body changed.
- Native demonstration tracks old `xy=-1/3` into new `zx=-1/3`. The row-only
  candidate fails its intended equality. Learner predictions remain untested.

### Pressure-unit checkpoint

- Lesson 7 adds nominal erased units and positive pressure scales. Five unit
  laws and five stress laws join the accumulated gate: explicit baseline,
  arbitrary component content, baseline zero, temporal and spatial diagonals.
- Native example at `q=3/4`, `P_c=12 Pa` yields values `3,4,−6,−4 Pa`;
  the intentionally wrong length scale is rejected at the pressure API.
- The source comparison explicitly preserves `P_c(U−η)` versus the Condensed
  `sigma+rho u u` bridge, and pressure versus force-density dimensions.
- Worker commit `49e4e86` preceded parent integration; `f2f3a7e` supplied the
  tracked unit dependency and coordinate checkpoint before pushing. No
  history rewrite or untracked dependency was used to land the checkpoint.

### Neighbour checkpoint

- Lesson 8 adds a pure, pressure-tagged discrete quotient over positive
  distance. Content and constant-field laws join the root gate.
- Added four constructive rational balance theorems for this and the following
  lessons. Polynomial certificates are backed by checked normalization
  soundness and a bridge to the existing rational operations, not axioms.
  No semantic-equality transitivity or universal multistep theorem is claimed.
- Native slope values are `2,1,−2,0 N/m³` for the documented spacing/order
  changes. The missing-spacing claim is rejected at its equality.
- Constant field, discrete secant slope and continuous derivative are kept
  separate; numerical convergence and learner mastery remain untested.

### Shared-face checkpoint

- Lesson 9 defines an explicit spatial-only `T_xi` flux selection, two
  equal-width cells, three shared face values, and negative-divergence gains.
  Ten contracts join the gate, including a universal boundary-balance theorem.
- Each cell computes its own gain; their actual sum is proved equal to the
  exterior-only expression. The shared interior reading is reused as `Data`
  across independent CPU parallel calls.
- Native closed/open examples yield `(2,−2)` / `(2,1) N/m³`. Swapping the
  transport and momentum indices is rejected on an asymmetric twist fixture.
- Native Bend 2.0.21 rejected the imported runtime basename `field-example`
  with a generated syntax error despite successful type checking. Renamed the
  shared fixture to `field.bend`; direct native demos then succeeded.
- The lesson explicitly separates conservative flux `−div Q` from isolated
  stress-force `+div tau`, and restricts the raising-sign argument to spatial
  indices in the declared flat Cartesian frame.

### Bounded-time checkpoint

- Lesson 10 consumes an affine two-cell momentum-density state and returns
  its updated state. Nonnegative duration and strictly positive width remain
  distinct domains; natural-number fuel decreases structurally.
- Seven new contracts specify duration, both updates, actual total,
  universal one-step boundary impulse, zero-fuel identity and run recurrence.
  The gate does not claim a separate all-fuel conservation theorem.
- Native closed-face runs from `(1,3)` give `(2,2)` after one half-second
  step and `(5,−1)` after four, with total 4. Four zero-time steps preserve
  `(1,3)`; an open-face step gives `(2,7/2)` and total `11/2`.
- The false claim that zero elapsed time erases momentum is rejected.
  Fuel bounds step count, not state magnitude, physical stability or runtime.
  The model remains explicitly frozen-forcing, without an invented RCCM closure.

### Repeatable verification and independent review

- Added `verify.py`: all ten native demos, ten intentional failures (diagnostic
  locations required), the earlier weak-law example and pressure regressions,
  plus 284 independent Python-Fraction API readings on native CPU threads 1/2.
- Twelve implementation mutants first pass standalone type checking and then
  fail unchanged contracts in isolated copies. Removing the evolution proof
  is rejected as incomplete. No production file is mutated by the runner.
- Preflight exposed `Unit` colliding with `Base.Unit` when checking the unit
  module itself. Renamed the new tag type to `UnitTag` in code and new-law
  binders, preserving the domain and propositions. Root and standalone checks
  now pass. All original 69 law bodies remain unchanged; current count is 112.
- Independent review of `16bdf78` approved the mathematical/source/proof route.
  It checked the polynomial helper's live soundness/realization chain and
  rejected a false certificate. Clean-archive checks found no missing imports.
  Its only non-blocking finding was the already-fixed standalone name collision.
- Review artifacts are in `tasks/code-review/rccm/lessons-6-10/16bdf78/`.
  Course navigation and the canonical atlas now distinguish delivered lessons
  from demonstrated learner understanding. No new learner probe was answered.
- Final clean archive `a323cb0`, launched from outside the live checkout,
  passed the complete `verify.py --mutations` workflow, including all 284
  native oracle readings on both thread counts. The committed package is
  self-contained; lesson links and line bounds also pass.
- Each delivery checkpoint was pushed without force to `local` `main`, with
  remote SHA checks. No `origin` publication or canonical TeX/HTML edit was
  performed. Only the pre-existing missing submodule remains in worktree status.

## Lessons 11–20 — Source fields, operators and Bend mastery

### Requested route and delivered checkpoints

- User requested ten additional asymmetric-tensor lessons for strong coders
  learning mathematics, a plan first, then implementation in sequence with
  commits and `git push local` throughout.
- Committed the jobs-to-be-done plan before implementation (`52ad21a`).
  Selected an exact source-reading laboratory rather than guessing a closed
  RCCM simulation. Each lesson received a separate verified and pushed commit.
- Delivered: nested budget and constructive capacity witness; unit-tagged
  slip/twist normalization; a polynomial expression AST and differentiator;
  Clebsch gradient/cross/curl; material transport and continuity residuals;
  flat-background index raising; sixteen-entry Lorentz contraction; planar
  strain compatibility; Laplacian/biharmonic manufactured sources; wave
  residuals and a polymorphic parallel template map.
- Bend techniques extend through erased unit tags, reusable syntax,
  structural recursion, equality composition, existential witnesses,
  balanced independent work, template specialization and tree induction.
  Exact arithmetic remains the shared reference; no second tensor
  implementation, new runtime dependency or unsafe proof escape was added.

### Sources and explicit boundaries

- Read the focused tensor source first, then the corresponding Condensed
  modal/action, motion and Rosetta sections. Neither TeX nor generated HTML
  changed.
- The focused Clebsch `lambda grad beta` sector is not automatically the
  Condensed `curl A + curl v_psi` decomposition. Adding transverse curl changes
  total vorticity without changing the internal Clebsch reading.
- Continuity removes a residual term; it does not justify the focused
  `rho_dyn -> rho_eff` substitution. Dividing by `rho_tau/alpha_a` also does
  not by itself remove `alpha_a` from the ambient acceleration term.
- The printed matrix with flat `(-,+,+,+)` raising gives `A:A=2(b²−e²)`.
  The focused `E²−B²` prose needs an explicit normalization/sign bridge.
- Polynomial compatibility, fourth-order and wave fixtures implement the
  written operators. They do not establish nonlinear curvature, action
  normalization, singularity resolution, closed dynamics or physical
  propagation. Initial/boundary data and constitutive feedback remain open.

### Verification and review follow-through

- Accumulated gate: 180 laws, retaining all original 112 law bodies unchanged.
  New declarations distinguish universal content contracts, general
  algebraic/inductive theorems and closed exact fixtures.
- Complete native gate passes: 20 demonstrations, 20 intended rejections,
  the earlier weak-law example and ten pressure regression cases.
- Independent Python `Fraction` expectations verify 284 earlier API readings
  plus 751 field/operator readings, each on CPU threads 1 and 2. Polynomial
  expectations use sparse coefficient algebra rather than the Bend AST.
  Batch checks preserve every leaf's path/order, including an unbalanced tree.
- 33 well-typed implementation mutants and a missing proof are rejected in
  disposable copies; production code/laws are never mutated by the runners.
- Independent review feedback found two useful gaps: comprehensive ordered
  coordinate-derivative coverage and the density-substitution boundary.
  Added the universal four-by-four coordinate contract and a selective
  wrong-axis mutant; clarified the source step in lesson 15 (`3143a37`).
  Review/verification subagents hit a provider limit before their final
  reports. Recovered the completed verifier edits and reran the full gate
  locally; do not treat the interrupted review as a final approval.
- Added `verify-course.py` as the single entry point for both suites.
  Checked 313 relative links/line-anchor bounds, excluding 16 pre-existing
  links into the absent cosmology submodule. Whitespace checks pass.
- A clean tracked `git archive` of the Bend subtree at `865252f` passed the
  complete `verify-course.py --mutations` gate from its extracted directory,
  including all 1,035 oracle readings on both CPU thread counts. No untracked
  source dependency was needed. `local/main` matched that verified commit.
  All checkpoints were pushed without force; `origin` was not published to.
  The only pre-existing worktree discrepancy remains the absent cosmology
  submodule, which was not staged or changed by this work.

### Learner map and return

- The curriculum is delivered, not marked mastered. No new prediction
  answers were supplied during this implementation request.
- Durable next move: lesson 11's nested-denominator probe. Hold `Pc=6` and
  static `3`; move one pressure unit from macro to local load. Predict both
  compliance squares, their product and the two diagonal readings.
- Later transfer gates: internal versus total curl; partial versus material
  derivative; raising versus transpose; zero scalar versus zero tensor;
  operator application versus solving for a field; universal law versus
  exact fixture. Keep the starting state explicit in every prediction.

## Graphical lessons — choose the destination before the route

- User explicitly changed the planning method: stop extending the lesson
  sequence one small step at a time. First choose the graphical experience
  with the greatest intuitive impact, then work backwards through recursively
  decomposed requirements, dependencies, gaps and work.
- Desired territory: CFD, the asymmetric tensor's gravitational-pressure and
  electromagnetic aspects in one visual experience, and experiments with
  Bend's parallel execution. The local Bend graphics demos are a concrete
  reference, not yet evidence of this application's performance.
- User requested maximum concurrent independent work and an adversarial,
  collaborative extreme-programming loop after the end goal and dependencies
  are clear. This turn starts with read-only concept reconnaissance, not an
  implementation swarm.
- Audience and winning scene are not yet agreed. A provisional audience is
  spatially curious technical adults/programmers, with optional deeper
  inspection for RCCM and physics readers.
- This is a change in preferred learning/project structure, not evidence of
  completing any additional mathematical prediction probes. The twenty
  lessons remain reusable contracts and references rather than prerequisites
  the user must finish before discussing the graphical destination.

### Concept reconnaissance and proposed return

- Three independent read-only passes examined Bend graphics/parallelism,
  scientific/source boundaries, and learning impact. The experience critique
  exposed a risk: an exact ordered-probe widget alone could be incremental
  lessons in prettier clothing.
- Added `docs/asymmetric-tensor-graphics.md` and linked it from the root atlas.
  Proposed destination: a transparent evolving continuum lab with a local
  tensor microscope. The central contrast is matched capacity with opposite
  selected twist, connected to ordered readings and explicit face exchanges.
  The proposal remains unapproved; it does not launch implementation tasks.
- The matched-state comparison is an authored fixture, not an asserted
  source-consistent sign edit or guaranteed pair of divergent physical futures.
  Same-direction cancellation concerns `A`, not the whole `U`; time-space
  slip is not a spatial-face traction. Coupled evolution and physical body
  response remain separate closure obligations.
- The inspected Bend reference HEAD was `6018e28`. Native interactive 2D/3D
  examples and documented Metal/CUDA paths support feasibility investigation.
  The pure parallel-sum example ran; no GUI, GPU or throughput result was
  measured for this proposed application.
- Existing `bend PROOF.bend` gate passed. All 15 proposal/navigation relative
  links and line-anchor bounds passed a local check; whitespace checks passed.
  No Bend laws, solver code, TeX, TauLab tasks or ideal-state files changed.
- An independent exact-rational calculation checked the proposal's selected
  matched-capacity fixture, cross-pair signs and split/probe identities.
  This checks the example, not a new universal theorem or renderer.
- Next decision: confirm whether the primary audience is visually curious
  technical adults or physics/CFD specialists, then choose the central scene.
  Only then reverse-decompose the observable outcome and schedule unblocked
  independent leaves with implementation/falsification review pairs.
- Independent proposal review found that the original acceptance criteria
  could all pass in a local probe bench despite the stated 3D-world ambition.
  Added mandatory world-level transfer gates: a whole-volume prescribed
  twist-reversal difference map with a distinct macro-load overlay, and a
  multi-step boundary-impulse/momentum-redistribution experiment across
  separated regions. The latter explicitly depends on the dynamics branch.
  Also clarified the two compared antisymmetric parts. The reviewer found
  no remaining math, source-boundary, performance or TauLab-boundary blocker
  after the earlier wording/link fixes; audience approval is still pending.

### Audience confirmed — Tom's toy, not a general-audience exhibit

- Tom explicitly confirmed an audience of exactly one person: himself.
  His long-term goals are deep mastery of RCCM, fluid dynamics generally,
  and massively parallel programming in Bend.
- His immediate highest priority is curiosity and excitement through his
  less-nerdy visual/aesthetic side: a toy he can play with that looks and
  feels good. He also explicitly requires it to be meaningfully real, build
  intuition, and not imply behavior that violates the theory.
- This supersedes the open audience question above. Updated the root atlas
  and graphical proposal; did not infer approval of a particular scene,
  numerical closure, interaction or implementation task graph.
- Reframed the recommendation as a beautiful fluid toy with a laboratory
  underneath. Free play is the default; tensor inspection, guided reversal,
  prediction questions and performance diagnostics are opt-in depth.
  Enjoyment must be confirmed with Tom, not inferred from attention metrics.
- Added an operational “real” contract: source-to-update-to-visible-response
  traceability; a fully specified bounded sector; meaningful, ledgered
  interventions; consistent derived tensor channels; domain/numerical checks;
  and aesthetic freedom that does not invent physical behavior.
- A prescribed-field or algebraic instrument can be real for its own limited
  question but cannot alone satisfy the evolving-fluid toy goal. Unsupported
  couplings remain blockers; a disclaimer does not make misleading default
  motion acceptable. Source ambiguities cannot be silently repaired for fun.
- No new mastery evidence, solver behavior or performance result was supplied.
  Next: identify the scene/gesture Tom wants to keep playing with and the
  source-supported dynamical regime that can make that interaction honest,
  then reverse-decompose the requirements.
- Verification: unchanged Bend proof gate still reports `All terms check.`;
  the root section keeps the source-router line positions unchanged.
  No application, physics implementation or task swarm was started.

## Foam — the first native graphical toy

### Request and consequential choice

- Tom requested an actual build: try to break the fluid into cavitation,
  invert fluid-as-stuff versus holes-as-stuff, watch seeded froth and overlay
  many time samples. He named Bret Victor as inspiration, authorized
  opinionated implementation, requested screenshots in Git, and explicitly
  requested pushes to both `local` and `origin`.
- Source reconnaissance found the pressure-capacity/yield boundary but no
  closed cavity nucleation, free-boundary or post-yield evolution law. A
  weak-field wave model would not honestly bridge that gap up to cavitation.
- Chose a **driven pressure-ledger instrument** as the first playable slice:
  nonnegative externally prescribed loads determine `q = P_static/Pc`.
  Moving seed stamps and the hand brush are declared drivers, not autonomous
  fluid motion or stochastic nucleation. The renderer's pockets are
  diagnostic level sets, not matter particles or material cavities.
- This delivers a real interactive instrument, not the full evolving-fluid
  destination. It does not silently relax that destination's closure gate.

### Delivered experience and implementation

- Added `bend/foam-lab/`: native Bend field, control laws/proofs, renderer,
  bitmap typography, bounded actual history, branching, native app, fixtures,
  image export, verification and a comparative rendering benchmark.
- Hold/drag adds a load, Tab inverts figure-ground without changing the
  field, Space holds/resumes, the timeline and brackets scrub saved states,
  O overlays up to three recorded contours, I probes inside the model domain,
  +/- changes only the contour, U withdraws the load, and R remixes the drive.
- The sampled minimum uses 64×64 points plus the brush center. A nonpositive
  recorded sample freezes that branch. The UI explicitly says `SAMPLE LIMIT`
  and `NONPOSITIVE CAPACITY SAMPLE`; no continuous-domain guarantee,
  reciprocal at zero, hidden pressure floor or post-yield dynamics is claimed.
- Bret Victor influence is functional: direct manipulation, two views of one
  state, a tangible timeline and simultaneously visible recorded history,
  not a mandatory lecture or quiz.
- Work split into independent source/graphics reconnaissance and disjoint
  control, field, typography and renderer implementation, followed by an
  adversarial code review and focused integration.

### Verification and review

- Native Bend 2.0.21 on Apple M1/Metal: both proof gates pass, including the
  toy's 19 exact control laws. Existing 180 course laws were not changed.
- Field regressions pass on CPU thread counts 1 and 4: 260,176 sampled
  baseline/replay cases, 720 gradient comparisons, compact support,
  monotonic loading, exact zero, unclamped overdraw and a wider-neighborhood
  reference. Largest tested gradient discrepancy was `0.00045585632`.
- Session tests cover real snapshots, bounded 180-frame storage, discarding a
  branch's newer future, display-only changes, input and frozen failure.
- Review found three actionable defects: half-pixel GUI/capture disagreement,
  out-of-domain inspector reads, and overly broad exhaustion wording. Fixed
  all three and added actual Image-leaf and inspector-domain regressions.
  The reviewer acknowledged closure of the findings.
- A same-evaluation CPU/GPU comparison could silently exercise CPU fallback.
  Replaced it with separate-process complete Image-tree comparison: all
  1,048,576 pixels agree between CPU threads 1/4; default Metal differs at
  17 pixels, by at most 1/255 per channel. This is measured near-parity,
  not bit identity or an F32 proof.
- The native smoke opens a real window, presents four frames and exits.
  `make test all smoke screenshots benchmark` passed. This is distinct from
  human interaction or taste validation by Tom.
- One final rendering batch measured warm CPU-1 times around 146–147 ms,
  CPU-4 around 42–44 ms and default GPU around 15–18 ms, with a 114 ms
  first GPU sample. These are render-plus-image-checksum measurements for
  one frozen fixture, not pure kernel timings, application FPS or CFD speed.
- Recorded four deterministic 1024×768 offscreen views from the same native
  pixel function and pixel-center convention as the GUI, with source and
  image hashes. macOS window capture was unavailable; no OS permissions or
  settings were changed. Python performs PNG encoding, not the visual model.

### Remaining boundary and return

- Canonical TeX, existing lesson implementations, TauLab's theory/solver and
  the pre-existing missing cosmology submodule were left untouched.
- No new mathematical mastery or enjoyment has yet been observed. Tom's next
  useful feedback is whether manipulating and inverting the field invites
  continued play; probes and formal inspection remain optional.
- The next scientific extension needs a source-supported dynamical closure,
  not merely more convincing bubble animation.
